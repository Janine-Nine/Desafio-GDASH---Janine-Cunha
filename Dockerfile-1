FROM node:20-alpine

WORKDIR /app

# Copy package files
COPY package*.json ./

# Install dependencies
RUN npm install

# Copy worker code
COPY index.ts .

# Install tsx globally
RUN npm install -g tsx

# Start the worker
CMD ["tsx", "index.ts"]
